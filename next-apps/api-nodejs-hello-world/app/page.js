export default function Home() {
	return (
		<main>
			<p>
				This application doesn't have a UI, just a single api route (running in
				the <code>node.js</code> runtime):
				<a href="/api/hello">
					<code>/api/hello</code>
				</a>
			</p>
		</main>
	);
}
